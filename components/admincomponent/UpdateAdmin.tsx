import { primaryColor } from "@/app/constants/color";
import { User } from "lucide-react";
import React from "react";

export default function UpdateAdmin() {
  return (
    <div>
      <User size={30} color={primaryColor}></User>
    </div>
  );
}
